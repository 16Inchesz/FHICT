#include <stdio.h>
#include <string.h>

int main()
    {   
    /*
    ------------------Assignment 1------------------------------- 
    const int ARRAY1SIZE = 4;
    const int ARRAY2SIZE = 5;
    const int ARRAY3SIZE = 10;
    
    int array_1[4] = {5, 4, 6, 8};
    int array_2[5] = {2, 1, 3, 4, 9};
    int array_3[10];

    for (int i = 0; i < ARRAY1SIZE; i++){
        array_3[i] = array_1[i];
    }
    for (int i = 0, j = ARRAY1SIZE; (j < ARRAY3SIZE) && (i < ARRAY2SIZE); i++, j++){
        array_3[j] = array_2[i];
    }
    
    array_3[5] = 7; 
    
    for (int i = 0; i < ARRAY3SIZE; i++){
        int result = array_3[i];
        printf("%d\n", result);
    }
    */

    
    //------------------Assignment 4------------------------------- 
    /*
    char sentence[1000];
    int count = 1, i;
    printf("enter a sentece: ");
    fgets(sentence, sizeof(sentence), stdin);


    int length = strlen(sentence);
    for (i = 0; i < length; i++){
        if (sentence[i] == ' '){
            count++;
        }
    }
    printf("number of words in sentence is %d\n", count);
    
    return 0;
    */
}
